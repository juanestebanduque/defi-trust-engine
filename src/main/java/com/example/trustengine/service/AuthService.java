package com.example.trustengine.service;

import org.springframework.stereotype.Service;

/**
 * AuthService — placeholder reservado para lógica auxiliar futura
 * (e.g. refresh tokens, logout, blacklist de tokens).
 * La lógica principal de autenticación está en {@link UserService}.
 */
@Service
public class AuthService {
    // Reservado para extensión futura
}
